<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto<% if (title) {%> - <%= title %> <% } %></title>
    
    
</head>
<body>

    <% if (typeof msg !== 'undefined') { %>
        <div class="alert alert-<%= msg.type %> text-center" role="alert">
            <%= msg.body %>
        </div>
    <% } %>

    <%- include('header.ejs') %>

    <% if (content) {%> <%- include(content) %> <%} %>
    
    <%- include('footer.ejs') %>
    
</body>



</html>